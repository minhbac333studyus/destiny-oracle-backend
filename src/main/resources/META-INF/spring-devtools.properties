# Fix NoClassDefFoundError for Spring AI Anthropic classes under DevTools RestartClassLoader.
# These packages must be loaded by the base classloader, not the restart classloader.
restart.include.spring-ai=/spring-ai-[\\w-]+\\.jar
